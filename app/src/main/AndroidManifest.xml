<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.vashkevich.lesson1hw">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lesson1HW">
        <activity android:name="HW5.UserPageActivity"></activity>
        <activity android:name=".HomeWork5Activity" />
        <activity android:name="HW2.AnimationActivity" />
        <activity android:name="HW2.FlagActivity" />
        <activity android:name=".HomeWork3Activity" />
        <activity android:name=".HomeWork2Activity" />
        <activity android:name=".HomeWork1Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>